<%= form_for(@qna) do |f| %>
  <% if @qna.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@qna.errors.count, "error") %> prohibited this qna from being saved:</h2>

      <ul>
      <% @qna.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="field">
    <%= f.label :answer %><br>
    <%= f.text_area :answer %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
